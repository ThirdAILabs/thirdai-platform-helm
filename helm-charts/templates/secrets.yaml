apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.licenseSecretName }}
type: Opaque
data:
  # Replace with real base64-encoded contents of your license file
  # TODO: Maybe replace this with a kubectl secret
  ndb_enterprise_license.json: >-
    ew
